services:
  db:
    image: postgres:16-alpine
    container_name: studesk-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "hTA[4K7PGkNBI+fgAm@snYrWxvW"
      POSTGRES_DB: studesk
    ports:
      - "5433:5432"
    volumes:
      - /home/volumes/studesk-db:/var/lib/postgresql/data

